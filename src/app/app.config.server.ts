// Minimal server config stub required by `src/main.server.ts`.
// The real project may provide providers for server rendering; this stub
// exports a safe default so the dev server can build.
export const config: any = {
  providers: []
};
